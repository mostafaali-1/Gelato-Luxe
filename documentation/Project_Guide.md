# دليل مشروع جيلاتو لوكس (Gelato Luxe) 🍦

مرحباً بك في دليل مشروع موقع **جيلاتو لوكس**! هذا المستند يوفر شرحاً مفصلاً لكامل الموقع، كيفية عمله، وهيكلة الملفات.

## 📌 نظرة عامة
هذا الموقع هو **متجر إلكتروني لبيع الآيس كريم (الجيلاتو)** المصنوع يدوياً. يهدف الموقع لتقديم تجربة مستخدم سلسة وجذابة لعرض المنتجات، إضافتها للسلة، وإتمام عملية الشراء بشكل وهمي، بالإضافة للتعريف بالبراند والتواصل مع العملاء.

التقنيات المستخدمة:
- **React**: لبناء واجهة المستخدم بشكل تفاعلي.
- **Vite**: لإدارة وإنشاء المشروع بسرعة.
- **CSS**: لتنسيق وتصميم الصفحات بشكل مخصص وجميل.

---

## 🚀 كيفية تشغيل المشروع
لتشغيل المشروع على جهازك، اتبع الخطوات التالية:

1. **تثبيت المكتبات:**
   افتح التيرمينال واكتب الأمر التالي لتثبيت جميع المكتبات اللازمة:
   ```bash
   npm install
   ```

2. **تشغيل الموقع:**
   بعد التثبيت، شغل السيرفر المحلي بالأمر:
   ```bash
   npm run dev
   ```
   سيظهر لك رابط (مثل `http://localhost:5173`)، اضغط عليه لفتح الموقع في المتصفح.

---

## 📂 هيكلة المشروع والملفات
المشروع منظم داخل مجلد `src` كالتالي:

### 1. **المجلد الرئيسي `src/`**:
- **`App.jsx`**: المكون الرئيسي الذي يجمع كل أجزاء الموقع. (Navbar, Hero, Products, ...الخ).
- **`main.jsx`**: نقطة البداية التي تقوم بربط تطبيق React بملف الـ HTML.
- **`index.css`**: يحتوي على التنسيقات العامة والخطوط والألوان الأساسية للموقع.

### 2. **المكونات `src/components/`**:
هذا المجلد يحتوي على جميع أجزاء الموقع، وكل مكون له ملف `.jsx` (للكود) وملف `.css` (للتصميم).

#### 🔹 **Navbar (شريط التنقل)**
- يعرض شعار الموقع وروابط الانتقال (Home, Flavors, Our Story).
- يحتوي على أيقونة سلة المشتريات مع عداد يوضح عدد المنتجات المضافة.
- يتغير لونه عند التمرير لأسفل الصفحة.

#### 🔹 **Hero (الواجهة الرئيسية)**
- القسم الأول الذي يراه الزائر.
- يحتوي على عنوان جذاب، وصف مختصر، وزر "تذوق الآن" لتشجيع المستخدم.
- يضم صورة متحركة (Animation) لآيس كريم لإعطاء طابع حيوي.

#### 🔹 **BestSellers (الأكثر مبيعاً)**
- يعرض المنتجات المميزة أو الأكثر طلباً.

#### 🔹 **Products (المنتجات)**
- يعرض قائمة بجميع نكهات الآيس كريم المتاحة.
- كل منتج يحتوي على صورة، اسم، سعر، وزر "إضافة للسلة".
- عند الضغط على "Add to Cart"، يتم إضافة المنتج لسلة المشتريات وتحديث العداد.

#### 🔹 **OurStory (قصتنا)**
- قسم يحكي قصة البراند وبداية المشروع.
- يركز على الجودة والمكونات الطبيعية.

#### 🔹 **AboutUs (من نحن)** *[تمت إضافته حديثاً]*
- قسم إضافي يعطي تفاصيل أكثر عن رؤية الشركة، التزامها بالجودة، ودورها في المجتمع.
- يحتوي على بطاقات (Cards) توضح القيم الأساسية مثل "مكونات مميزة"، "صنع بحب".

#### 🔹 **Testimonials (آراء العملاء)**
- يعرض تقييمات وتعليقات من عملاء سابقين لزيادة الثقة.

#### 🔹 **Newsletter (النشرة البريدية)** *[تمت إضافته حديثاً]*
- نموذج بسيط يسمح للمستخدم بإدخال بريده الإلكتروني للاشتراك في العروض والأخبار.
- عند الاشتراك، تظهر رسالة شكر "تنبيه".

#### 🔹 **Cart (سلة المشتريات)**
- قائمة جانبية تظهر عند الضغط على أيقونة السلة.
- تعرض جميع المنتجات التي تم اختيارها مع إجمالي السعر.
- يمكن حذف منتجات منها.
- تحتوي على زر "Proceed to Checkout" للانتقال للدفع.

#### 🔹 **Checkout (الدفع)**
- نافذة منبثقة (Modal) لإتمام الشراء.
- تطلب من المستخدم إدخال بياناته (الاسم، العنوان، البطاقة البنكية).
- تقوم بمحاكاة عملية دفع (تأخذ وقت قصير ثم تظهر رسالة نجاح).
- **ملاحظة:** عملية الدفع ليست حقيقية، هي فقط محاكاة للعرض.

#### 🔹 **Footer (تذييل الصفحة)**
- الجزء السفلي للموقع، يحتوي على حقوق النشر ومعلومات سريعة.

---

## 🎨 التصميم (CSS)
تم استخدام CSS عادي (Plain CSS) لضمان سهولة التعديل والفهم.
- **Variables**: تم استخدام متغيرات CSS في `index.css` للألوان والخطوط لضمان تناسق التصميم في كامل الموقع.
- **Responsive**: الموقع متجاوب ويعمل بشكل جيد على جميع أحجام الشاشات (كمبيوتر، تابلت، موبايل).

---

نتمنى أن ينال المشروع إعجابكم! لأي استفسارات أخرى، لا تتردد في السؤال.
